# Laravel Docker

## Requirements
TBD

## Usage

```
# Build / Up
$ docker-compose build
$ docker-compose up -d

# PHP
$ docker-compose exec php ash

# MySQL
$ docker-compose exec mysql bash

# Kill
$ docker-compose kill

# Remove
$ docker-compose rm

# Process
$ docker-compose ps
```
